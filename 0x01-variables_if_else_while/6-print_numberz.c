#include <stdio.h>
/**
 * main - main block
 * Description: prints single digits from 0 to 10
 * starting from zero, followed by a new line.
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n)
	}

	putchar('\n');

	return (0);
}
